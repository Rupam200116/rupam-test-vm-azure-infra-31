# rupam-test-vm-azure-infra-31

**Cloud:** AZURE
**App:** rupam test vm
**Requirements:** create a vm

Auto-generated by [InfraAgent](https://github.com).

## Authentication

This repo authenticates to Azure using a **Service Principal client secret** (not OIDC) — the secret is encrypted at rest by GitHub and masked in all logs, but it is a long-lived credential, so rotate it periodically in Entra ID.

Secrets are set automatically by InfraAgent:

| Secret | Value |
|--------|-------|
| `AZURE_CLIENT_ID` | set automatically from .env |
| `AZURE_CLIENT_SECRET` | set automatically from .env |
| `AZURE_TENANT_ID` | set automatically from .env |
| `AZURE_SUBSCRIPTION_ID` | set automatically from .env |
| `AZURE_CREDENTIALS` | set automatically from .env |

**Actions → Deploy Infrastructure (AZURE) → Re-run jobs** to redeploy.
